{
  "name": "fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix scraping-api\" \"npm run dev --prefix nextjs\"",
    "install:all": "npm install --prefix scraping-api && npm install --prefix nextjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
